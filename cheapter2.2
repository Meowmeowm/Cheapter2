#include <iostream>
using namespace std;

int distanceChange(double);
int main()
{
    double lang;
    cout<<"enter the distance of lang: ";
    cin>>lang;
    double ma =distanceChange(lang);
    cout<< lang<<"lang = ";
    cout<< ma <<" ma."<<endl;
    return 0;
}
int distanceChange(double sts)
{
    return  sts;
}
